service: surebank-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  timeout: 90 
  environment: ${file(env.json)}

functions:
  api:
    handler: sls.handler
    events:
      - httpApi: '*'

plugins:
  - serverless-offline
